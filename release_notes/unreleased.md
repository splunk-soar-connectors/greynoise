**Unreleased**

* Escaped API-controlled values before embedding them in GreyNoise widget JavaScript contexts.
* Rejected malformed CVE IDs before sending requests to the GreyNoise API.
* Require a configured shared token on every GreyNoise webhook request.
